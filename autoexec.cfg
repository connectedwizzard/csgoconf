// Launch - set for 240 fps and 240 hz monitor
// +fps_max 240 -refresh 240 -forcenovsync -high -novid -nojoy -softparticlesdefaultoff +violence_hblood 0 +r_drawparticles 0 -console +mat_disable_fancy_blending 1 +r_dynamic 0 +mat_queue_mode 2 -tickrate 128 +exec autoexec.cfg

// Crosshair
//CSGO-UiGRT-sps3Y-dfdOO-hvvyB-SjG4C

// Exec
exec overrides/gfx
exec overrides/filter
exec overrides/scripts
exec overrides/crosshair

// Alias
alias "q" "quit"
alias "d" "disconnect"
alias "c" "connect"
alias "k" "kill"
alias "e" "exec"

// Bind
bind "k" kill

// Echo
echo "loaded config"
echo "loaded config"
echo "loaded config"
echo "loaded config"
echo "loaded config"
echo "loaded config"
